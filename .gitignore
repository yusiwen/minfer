/target/
*.gguf
/tmp/
*.npy
*.f32
*.log
.env
.qoder/
.venv/
__pycache__/
llama_ref/
uv.lock
.direnv/
# CocoIndex Code (ccc)
/.cocoindex_code/
/.cargo-home/
/.ccc_config/

# mdBook documentation site build output
/book/

# Mermaid assets for the mdBook build: staged by scripts/build_book.sh (downloaded
# and sha384-checked) and removed again when it exits — never committed, see book.toml
/docs/assets/mermaid.min.js
/docs/assets/mermaid-init.js

# git-hooks.nix auto-generated pre-commit config (symlink into the nix store)
/.pre-commit-config.yaml

# Nested git worktrees for agent/parallel sessions (see AGENTS.md). They must be ignored: without
# this line the outer tree's `git add -A` warns "adding embedded git repository" and stages the
# worktree as a gitlink. Each one keeps its own /target/ (ignored by its own copy of this file).
/.worktrees/

# stray MINFER_TRACE dump accidentally committed twice (Phase 8 review)
/1
